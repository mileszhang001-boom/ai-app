# AI小组件 · 创意工坊 — 产品 Spec

> **定位**：本文档是产品完整 spec，供开发团队参考。
> **目标**：用一个可运行的 Demo 向高层证明「AI小组件」方向的可行性与价值。
> **原则**：极端克制，只做验证核心假设所必需的最小闭环。

---

## v0.3 更新说明 (2026-03-22)

手机端 4 屏全面改版，重构核心交互流程：

| 变更 | 旧版 (v0.2) | 新版 (v0.3) |
|------|------------|------------|
| 首页 | Hero大标题 + textarea + emoji场景标签 + 底部导航 | 状态栏 + 品牌Header + 3×3彩色图标宫格 + 底部单行输入 |
| 配置 | 独立页面 `config.js`（手动填表） | Bottom Sheet 组件 `ConfigPanel`（场景配置 + 色圆） |
| 预览 | 深色 bezel 外壳 + 内联微调弹窗 | 浅色卡片 + AI摘要/洞察条 + 双按钮（微调 + 同步） |
| 微调 | 预览页底部 textarea 弹窗 | 独立微调页（缩小预览 + 嵌入式配置面板 + AI建议） |
| 导航 | 底部 Tab 导航（模板/我的/同步） | 无 Tab，线性流程：首页 → 配置 → 预览 → 微调 |
| 生成流程 | 输入 → 分析气泡 → 确认 → 生成 | 输入 → 4步动效 → 直达预览（去掉确认步骤） |

**新增文件**：`components/config-panel.js`、`pages/finetune.js`、`utils/render-widget.js`

## v0.2 更新说明 (2026-03-21)

原 v0.1 Spec（下文）定义了 3 种组件类型（纪念日/新闻/闹钟），v0.2 已扩展至 **9 个模板**：

| 新增模板 | 说明 | 对应 Spec 中的位置 |
|---------|------|-------------------|
| 暖橙纪念 (warm) | 纪念日第4主题，琥珀色调 | 纪念日类目扩展 |
| 天气卡片 (weather) | 动态天气 + 3日预报 + AI穿衣建议 | 新增实用类 |
| 音乐播放器 (music) | 专辑封面 + 频谱 + 播放控制 | 新增实用类 |
| 日历日程 (calendar) | 农历 + 时间线 + 事件倒计时 | 新增实用类 |

**设计语言升级**：从 "Ambient Light" 升级为 **Liquid Glass**（Glassmorphism 毛玻璃 + 呼吸光晕 + 粒子系统 + 数字翻牌）。对标 Apple CarPlay iOS 26。

**竞品参考**：理想汽车「桌面大师」(OTA 8.0) 是唯一直接竞品，采用 AI 全代码生成（慢且质量不稳定）。我们的"精品模板 + AI 参数化"方案在速度和视觉质量上有明确优势。

详细技术进度见 `TECH_PLAN.md`。

---

## 一、Demo 产品亮点与用户故事

### 1.1 一句话

**"告诉我你想要什么，AI 帮你生成一张专属的车机桌面卡片。"**

### 1.2 核心亮点

| # | 亮点 | 打动点 |
|---|------|--------|
| 1 | **秒级生成** | 用户填完参数，≤5s 看到一张可运行的车机卡片，现场感知 AI 的能力 |
| 2 | **所见即所得** | 手机端预览严格模拟车机 1/3 屏卡片（896×1464），确认后秒级同步到车 |
| 3 | **精致如原生** | 视觉品质与车机系统应用一致——精致、简洁、风格匹配，不是"能跑就行"的毛坯 |
| 4 | **个性化表达** | 同一个"纪念日"组件，每个人的主题、配色、文案都不一样——我的车桌面就是我的作品 |

### 1.3 v0.1 组件类型总览

v0.1 包含 **2 大类、共 4 种组件**：

| 类别 | 组件 | 说明 |
|------|------|------|
| 🎯 情感类 | **纪念日** | 统一的"纪念日"组件，内含 3 个主题方向（见下方用户故事） |
| 🔧 实用类 | **每日新闻** | 每天自动更新的新闻摘要卡片 |
| 🔧 实用类 | **闹钟** | 快捷设置闹钟 + 下一个闹钟时间展示 |

#### 纪念日组件的 3 个主题

纪念日是一个统一组件，支持"倒数"和"正数"两种模式。通过不同的主题预设覆盖多种场景：

| 主题 | 模式 | 典型场景 | 情感内核 |
|------|------|---------|---------|
| 💕 恋爱纪念 | 正数（在一起第X天） | 恋爱、结婚纪念日 | 甜蜜、温暖 |
| 👶 宝宝成长 | 正数（出生第X天） | 宝宝出生、重要里程碑 | 珍惜、成长 |
| 🏖️ 放假倒计时 | 倒数（距离X还有X天） | 国庆、春节、年假 | 期待、活力 |

### 1.4 Demo 演示用户故事（建议准备 3 个）

#### 故事 A：恋爱纪念日（情感共鸣 → 自传播）

> 小王和女朋友的三周年纪念日快到了。他打开手机上的「AI小组件」，选择"纪念日"模板下的"恋爱纪念"主题，输入日期和标题"在一起的第___天"，AI 自动生成了一句副标题"每一天都算数"，他选了一个温暖的渐变风格。不到5秒，一张精美的纪念日卡片生成了。他点击"同步到车机"，下次上车时，行车桌面上就出现了这张卡片，每天数字都在跳动。女朋友坐上副驾看到，截图发了朋友圈。

**打动管理层的点**：自传播。用户自发截图分享 → 品牌曝光 → 潜在购车转化。

#### 故事 B：放假倒计时（高频共鸣 → 社交话题）

> 打工人小赵距离国庆假期还有 47 天，他用"纪念日"模板的"放假倒计时"主题，创建了一个国庆倒计时。AI 自动生成了副标题"假期在向你招手 🏖️"，配了一个明亮活力的风格。每天上车通勤，那个不断减小的数字就是最大的精神支柱。同事看到了也想搞一个——"你这个怎么弄的？"

**打动管理层的点**：天然社交话题。每逢假期前都是传播高峰，可运营节日主题活动。

#### 故事 C：宝宝成长记录（情感 + UGC 飞轮）

> 新手爸爸老张给刚出生的宝宝创建了一个"宝宝出生第___天"的成长卡片，配了一个柔和的主题风格。每天上车看到数字增长，是一种小确幸。车友群里有人问"你这个桌面卡片怎么弄的"，飞轮开始转。

**打动管理层的点**：UGC 飞轮雏形。用户好奇 → 下载使用 → 自己创建 → 再传播。

### 1.5 Demo 演示脚本建议

整个演示控制在 **3 分钟以内**，节奏如下：

1. **30s 讲痛点**："车载商店应用少，开发者不愿来，但用户有大量长尾个性化需求"
2. **60s 现场演示**：打开手机 → 首页 8 个场景图标一目了然 → 点击"实时天气" → 配置面板选风格/配色 → "生成卡片" → 4步AI创作动效 → 预览页卡片渲染完成 → "同步到车机" → 成功动画
3. **30s 展示多样性**：返回首页 → 快速点击不同场景（恋爱/日程/音乐/新闻）→ 每个都能秒级生成，证明覆盖广度
4. **60s 讲故事**：小米车主群体特征（年轻、科技、分享欲） → UGC 飞轮 → 商业可能性（模板市场、节日主题运营）

---

## 二、关键体验打磨点 & 技术 PoC 重点

### 2.1 必须打磨的体验（Demo 成败取决于此）

#### ⭐ P0: 视觉品质——精致、简洁、与车机风格匹配

这是 Demo 的"第一印象"，必须投入最多精力打磨。

**核心审美标准**：
- **精致**：细节经得起推敲——字间距、色彩过渡、元素对齐、圆角一致性
- **简洁**：1/3 屏卡片上信息不超过 3 层（主数字 + 标题 + 副标题），留白充分
- **与车机匹配**：色调、字体、圆角、阴影等视觉语言与车机系统 UI 一脉相承，不突兀

**具体要求**：
- 字体层级清晰：主数字（天数/倒计时）极大且醒目，副标题适中，辅助信息最小
- 配色和谐：每种预设风格都是设计师调过的完整色板，不允许 AI 自由选色
- 暗色模式优先：车机绝大多数时间处于暗色主题，所有组件必须在暗色下优先适配
- 动效克制但有品质感：数字跳动、渐入等微动效，要精致不要花哨
- 严格适配 896×1464 卡片尺寸：竖向布局，内容居中，上下留白
- 每个组件的每种风格，在不同内容长度下都不能"破版"（标题过长时截断/缩字）

**实现策略**：
- 不依赖 AI 从零生成 CSS，AI 只选择预定义的「风格包」+ 填充用户参数
- 每种组件类型 × 每种风格 = 一个精心设计的 H5 模板，AI 做参数注入
- Design Tokens 强制注入且不可覆盖，保证视觉底线
- **设计师必须逐一审核每个模板×风格的渲染效果**，签字确认后才纳入

#### ⭐ P0: 生成速度

**目标**：从用户点击"生成"到看到预览 ≤ 5 秒。

**拆解（L1 模板参数化方案）**：
- AI 推理（理解意图 + 输出 JSON 参数）：≤ 2s
- 前端模板引擎渲染 H5：≤ 1s
- 预览加载：≤ 1s
- 预留 buffer：1s

L1 方案下 AI 不写代码，只输出 JSON，所以生成速度很快，5s 是保守估计。

#### P1: 手机端预览的真实感

**目标**：手机端预览 ≈ 车机实际效果，减少"到车上一看不一样"的落差。

**具体要求**：
- 预览区域严格按 896×1464 比例渲染（260×434 等比缩放卡片）
- 使用和车机完全一致的 Design Tokens 和字体
- 浅色背景 + 圆角阴影卡片，聚焦内容本身
- AI 摘要条展示生成结果描述，AI 洞察条展示模板能力亮点
- 预览页通过 iframe srcdoc 加载真实 H5 模板，确保所见即所得

#### P1: 同步到车的"仪式感"

**目标**：用户点击同步后，有明确的成功反馈。

**具体要求**：
- 手机端：同步动画 + "已同步到你的车" 提示
- 车端：新组件首次出现时有微妙的入场动画（淡入即可）
- 如果车机不在线，手机端提示"已保存，下次启动车辆时自动同步"

### 2.2 技术 PoC 验证重点

| # | PoC 项目 | 验证目标 | 成功标准 | 失败的后备方案 |
|---|---------|---------|---------|--------------|
| 1 | AI 参数生成可用性 | AI 能否准确理解用户意图并输出合法 JSON 参数 | JSON 合法率 ≥ 99%，意图理解准确率 ≥ 95% | 简化 AI 角色为纯文案生成，参数全由用户手填 |
| 2 | 车端 WebView 渲染性能 | 896×1464 WebView 加载和渲染 H5 组件的性能 | 首屏渲染 ≤ 2s，内存占用 ≤ 50MB，60fps | 精简 H5 复杂度；预渲染为静态图片 |
| 3 | 跨端同步链路 | 手机创建 → 云端 → 车端推送的全链路时效 | 车机在线时端到端 ≤ 5s | 降级为手动刷新/轮询 |
| 4 | 每日新闻数据源 | 新闻 API 的稳定性和内容质量 | 每日可获取 ≥ 5 条有效新闻摘要 | 退化为 RSS 聚合或人工运营 |

---

## 三、技术难点与建议选型

### 3.1 整体技术栈建议

```
┌─────────────────────────────────────────────────────┐
│                    手机端（创建）                       │
│  框架: H5 Web App（最快交付）/ React Native（体验优先）  │
│  核心页面: 模板市场 → 参数配置 → AI生成 → 预览 → 同步     │
└──────────────────────┬──────────────────────────────┘
                       │ HTTPS
┌──────────────────────▼──────────────────────────────┐
│                    云端服务                            │
│  AI 生成服务: Python (FastAPI)                        │
│  同步服务: WebSocket / MQTT 推送                       │
│  新闻服务: 新闻API聚合 + 缓存                           │
│  存储: 对象存储(H5产物) + KV(组件元数据)                  │
└──────────────────────┬──────────────────────────────┘
                       │ MQTT / WebSocket
┌──────────────────────▼──────────────────────────────┐
│                    车端（运行）                         │
│  宿主应用: Android App（AI小组件）                      │
│  渲染引擎: Android WebView (Chromium内核)              │
│  桥接层: JSBridge（白名单API）                          │
│  展示: 行车桌面 1/3 屏卡片（896×1464）                   │
└─────────────────────────────────────────────────────┘
```

**手机端选型说明**：
- **不使用小米快应用**（明确排除）
- **优先推荐 H5 Web App**：开发速度最快，跨平台天然支持，Demo 阶段首选。可通过小米应用内 WebView 或独立浏览器访问
- **备选 React Native**：如果团队有 RN 经验且对交互体验要求更高
- 核心考量：Demo 阶段追求**最快交付**，手机端不是 Demo 重点（车端卡片效果才是）

### 3.2 核心技术难点逐项分析

#### 难点 1：AI 生成方案——L1 模板参数化详解

**问题**：AI 完整生成 H5 代码（HTML+CSS+JS）的质量波动大，且延迟高。

**v0.1 采用 L1 纯模板参数化方案**。下面用一个完整的例子说明 L1 的输入、处理过程和输出：

##### L1 完整流程示例

```
场景：用户想创建一个"放假倒计时"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 1: 用户在手机端操作
┌─────────────────────────────┐
│  用户选择: 纪念日模板          │
│  用户选择: 放假倒计时主题       │
│  用户填写:                    │
│    标题: "国庆快乐"            │
│    日期: 2025-10-01           │
│    副标题: (留空，让AI生成)     │
│  用户选择: 风格 → "活力橙"     │
│  点击 [生成]                  │
└──────────────┬──────────────┘
               │
               ▼
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 2: 发送给 AI（输入）

  System Prompt（预置，包含模板清单和规则）
  +
  User Message：
  "用户选择了纪念日组件-放假倒计时主题，
   标题是'国庆快乐'，日期是2025-10-01，
   风格选了'活力橙'，副标题留空需要你生成。
   请输出 JSON 参数。"

               │
               ▼
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 3: AI 处理（~1-2秒）

  AI 做了什么:
  ✅ 确认组件类型 → anniversary (countdown mode)
  ✅ 确认模板 → anniversary_countdown_vibrant
  ✅ 确认风格 → vibrant_orange
  ✅ 确认用户已填参数 → title, target_date
  ✅ 生成缺失的副标题 → "假期在向你招手"
  ❌ AI 不做: 不写HTML，不写CSS，不写JS

               │
               ▼
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 4: AI 输出（一段 JSON）
```

```json
{
  "component_type": "anniversary",
  "mode": "countdown",
  "theme": "holiday",
  "template_id": "anniversary_countdown_vibrant",
  "style_preset": "vibrant_orange",
  "params": {
    "title": "国庆快乐",
    "target_date": "2025-10-01T00:00:00",
    "subtitle": "假期在向你招手",
    "show_hours": false,
    "show_emoji": true
  }
}
```

```
               │
               ▼
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 5: 前端模板引擎渲染（~1秒）

  1. 根据 template_id 找到对应的 H5 模板文件
     → templates/anniversary_countdown_vibrant/index.html
  2. 根据 style_preset 加载对应的风格CSS
     → styles/vibrant_orange.css
  3. 将 params 中的参数注入模板
     → {{title}} 替换为 "国庆快乐"
     → {{target_date}} 替换为 "2025-10-01T00:00:00"
     → {{subtitle}} 替换为 "假期在向你招手"
  4. 渲染出完整的 H5 页面 → 展示预览

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

总耗时: 用户点击生成 → 看到预览 ≈ 2-3 秒
```

##### AI 在 L1 中的价值

虽然 L1 看起来 AI 只输出了 JSON，但 AI 在以下场景中的价值不可替代：

| 场景 | AI 做了什么 | 没有 AI 的话 |
|------|-----------|-------------|
| 副标题为空 | AI 根据主题和日期生成有创意的文案 | 用户必须自己想文案 |
| v0.5 自然语言输入 | "帮我做一个国庆倒计时" → AI 自动选模板、选风格、填参数 | 用户必须手动选择每一项 |
| v0.5 智能推荐 | 临近春节时 AI 主动推荐"要不要做个春节倒计时？" | 纯被动等用户操作 |

##### 三级生成策略演进

| 级别 | 方式 | AI 输出 | 适用阶段 |
|------|------|--------|---------|
| L1 纯模板 | AI 输出 JSON 参数 → 前端模板引擎渲染 | JSON（参数+文案） | v0.1 Demo |
| L2 模板+微调 | AI 基于模板骨架生成局部 CSS 变体 | JSON + CSS 片段 | v0.5 |
| L3 自由生成 | AI 完整生成 H5 代码 | HTML+CSS+JS | v1.0+ |

#### 难点 2：车端 WebView 性能与稳定性

**问题**：车机 Android WebView 版本可能较低、内存受限、需长时间运行不崩溃。

**卡片尺寸确认**：896×1464 逻辑像素（1/3 屏）。

**建议**：
- 确认车机 WebView 内核版本（Chromium 版本），决定可用的 CSS/JS 特性集
- H5 组件必须极度轻量：单组件总资源 ≤ 100KB（含 HTML+CSS+JS+图片）
- 禁止使用重型框架（React/Vue），用 Vanilla JS + CSS 实现
- 所有动效用 CSS Animation / requestAnimationFrame，禁用 JS 定时器密集更新
- 内存泄漏防护：组件生命周期管理，不可见时暂停所有定时器和动画
- WebView 崩溃隔离：宿主 App 捕获 WebView 崩溃，自动重载而非影响桌面

**建议做一次基准测试**：
```
测试项: 在目标车机上运行一个包含 CSS 动画的 H5 页面（896×1464 WebView）24 小时
观测指标: 内存趋势、帧率、WebView 崩溃次数
目标: 内存无持续增长，帧率稳定 60fps，0 崩溃
```

#### 难点 3：跨端同步

**问题**：手机创建后如何快速、可靠地将组件推送到车机。

**建议方案**：

```
手机端                    云端                      车端
  │                        │                        │
  │── 1.上传组件产物 ──────►│                        │
  │   (H5 zip + metadata)  │                        │
  │                        │── 2.推送通知 ──────────►│
  │                        │   (MQTT/长连接)          │
  │                        │                        │── 3.拉取组件产物
  │                        │◄───────────────────────│
  │                        │── 4.返回 H5 zip ──────►│
  │                        │                        │── 5.解压 + 加载
  │◄── 6.同步确认 ─────────│◄── 确认完成 ───────────│
```

**关键设计决策**：
- 组件产物以 zip 包形式存储（HTML + CSS + JS + 图片资源）
- 云端存储用对象存储（如 S3/MinIO），元数据用 KV/MySQL
- 车端组件缓存在本地，支持离线运行
- 车机首次启动时全量同步，之后增量更新
- 如果车机团队已有推送通道（MQTT/自有长连接），优先复用，不新建

**Demo 阶段简化方案**：
如果推送通道搭建成本高，Demo 可以用轮询替代（车端每 5s 检查一次有无新组件），体验可接受且大幅降低开发成本。

#### 难点 4：Design Tokens 从 Figma 到代码的转换

**问题**：现有 Figma 组件库需要翻译成 H5 可执行的形式。

**建议工具链**：

```
Figma 组件库
    │
    ▼ (Figma Tokens 插件 / Style Dictionary)
CSS Variables 文件 (design-tokens.css)
    │
    ├──► H5 模板引用（直接 link）
    │
    └──► AI System Prompt 中注入（作为约束上下文）
```

**v0.1 最小 Token 集合**：

```css
/* ── 色彩（暗色主题优先） ── */
--color-bg-primary: #1A1A2E;
--color-bg-secondary: #16213E;
--color-bg-card: #1E1E3A;
--color-text-primary: #FFFFFF;
--color-text-secondary: rgba(255,255,255,0.7);
--color-text-tertiary: rgba(255,255,255,0.4);
--color-accent: #FF6900;       /* 小米橙 */

/* ── 字号（896×1464 卡片内，车载可读性优先） ── */
--font-size-hero: 72px;        /* 核心数字（天数/倒计时） */
--font-size-title: 28px;       /* 标题 */
--font-size-body: 20px;        /* 正文/副标题 */
--font-size-caption: 16px;     /* 辅助信息 */

/* ── 间距 ── */
--spacing-xs: 8px;
--spacing-sm: 16px;
--spacing-md: 24px;
--spacing-lg: 40px;
--card-padding: 32px;          /* 卡片内边距 */

/* ── 安全约束 ── */
--button-min-height: 88px;     /* 车载最小可触面积 */
--contrast-ratio-min: 4.5;     /* WCAG AA */
--border-radius-card: 16px;
--border-radius-element: 12px;
```

#### 难点 5：每日新闻数据源

**问题**：新闻组件需要每日更新的内容源。

**建议方案**：
- 云端新闻聚合服务，每日定时拉取 → AI 摘要 → 缓存
- 数据源优先级：官方新闻 API（如头条/腾讯新闻开放平台）> RSS 聚合 > 人工运营
- 车端组件每次展示时从云端拉取最新缓存（非实时请求新闻源）
- 分类支持：综合、科技、汽车（v0.1 只做综合即可）

**Demo 简化方案**：
如果新闻 API 接入周期长，Demo 可以用预置的模拟数据（每天手动更新几条），核心是验证卡片上新闻的展示体验。

#### 难点 6：行车桌面卡片内多组件切换

**问题**：1/3 屏卡片只能展示一个组件，但用户可能有多个。

**v0.1 方案：手动点击切换**

不做智能排序，采用最简单可靠的方案：
- 卡片底部显示圆点指示器（· · ● · ·），标示当前是第几个组件
- 卡片上方左右各一个轻量的切换箭头按钮（← →），点击切换上一个/下一个
- 组件顺序 = 用户创建顺序（最新的在最前面），支持在手机端拖拽排序
- 用户上次查看到第几个组件，下次上车自动恢复到同一个

**交互细节**：
- 切换箭头默认半透明，触摸时高亮，3秒无操作后淡出（减少视觉干扰）
- 切换动画：简单的左右滑动过渡，200ms duration
- 如果只有 1 个组件，不显示箭头和圆点

### 3.3 技术选型总结

| 模块 | 推荐选型 | 备选 | 选型理由 |
|------|---------|------|---------|
| AI 生成 | LLM API (GPT-4o / Claude) + JSON Mode | 自建微调模型 | Demo 阶段追求速度和灵活性，不值得训练模型 |
| AI 服务框架 | Python FastAPI | Node.js | Python 生态对 LLM 集成更成熟 |
| H5 模板引擎 | Vanilla JS + Handlebars/Mustache | Lit/Preact | 最轻量，兼容性最好，车机 WebView 友好 |
| 手机端 | H5 Web App（最快交付） | React Native | Demo 阶段手机端不是重点，追求最快上线 |
| 云端存储 | 对象存储 + MySQL/Redis | MongoDB | 组件产物用对象存储，元数据结构简单用关系型 |
| 新闻数据源 | 新闻 API + AI 摘要 + 缓存 | RSS / 人工运营 | Demo 可先用模拟数据，后续接入真实源 |
| 推送同步 | MQTT（如已有通道）/ 轮询（Demo降级） | WebSocket | 复用车机已有推送通道成本最低 |
| 车端宿主 | Android App + WebView | 小程序容器 | WebView 是最直接的方案，后续可升级 |

---

## 四、产品架构与后续迭代计划

### 4.1 v0.1 Demo 架构

```
┌─────────────────────────────────────────────────────────────┐
│                        手机端                                │
│                                                             │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌─────────┐  │
│  │ 首页     │──►│ 配置面板  │──►│ 预览     │──►│ 微调    │  │
│  │ 8场景宫格 │   │ BottomSheet│  │ AI摘要+  │   │ 缩小预览│  │
│  │ +自由输入 │   │ 风格/配色  │  │ 卡片渲染  │   │ +配置面板│  │
│  └──────────┘   └──────────┘   └────┬─────┘   └─────────┘  │
│                                      │                       │
│                           ┌──────────▼────┐                  │
│                           │  同步到车机    │                  │
│                           └──────────┬────┘                  │
└─────────────────────────────────────────────────────┼───────┘
                                                      │
┌─────────────────────────────────────────────────────▼───────┐
│                        云端                                  │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ AI 生成服务   │  │ 组件存储服务  │  │ 同步推送服务  │       │
│  │ - LLM API    │  │ - H5 zip    │  │ - MQTT推送   │       │
│  │ - Prompt管理  │  │ - 元数据     │  │ - 状态确认   │       │
│  │ - JSON校验   │  │              │  │              │       │
│  └──────────────┘  └──────────────┘  └──────┬───────┘       │
│                                              │               │
│  ┌──────────────┐                            │               │
│  │ 新闻聚合服务  │                            │               │
│  │ - API拉取    │                            │               │
│  │ - AI摘要     │                            │               │
│  │ - 定时缓存   │                            │               │
│  └──────────────┘                            │               │
└──────────────────────────────────────────────┼───────────────┘
                                               │
┌──────────────────────────────────────────────▼───────────────┐
│                        车端                                   │
│                                                              │
│  ┌──────────────┐   ┌──────────────┐   ┌─────────────────┐   │
│  │ 组件管理服务  │──►│ WebView 容器  │──►│ 行车桌面卡片     │   │
│  │ - 下载/缓存  │   │ - 沙箱渲染    │   │ - 896×1464      │   │
│  │ - 本地存储   │   │ - JSBridge   │   │ - 手动←→切换     │   │
│  └──────────────┘   └──────────────┘   └─────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 v0.1 核心数据模型

#### 组件元数据 (Component)

```json
{
  "id": "comp_uuid",
  "user_id": "user_uuid",
  "type": "anniversary",
  "mode": "countdown",
  "theme": "holiday",
  "template_id": "anniversary_countdown_vibrant",
  "style_preset": "vibrant_orange",
  "params": {
    "title": "国庆快乐",
    "target_date": "2025-10-01T00:00:00",
    "subtitle": "假期在向你招手",
    "show_hours": false
  },
  "sort_index": 0,
  "h5_bundle_url": "https://oss.xxx.com/widgets/comp_uuid.zip",
  "created_at": "2025-06-01T10:00:00Z",
  "updated_at": "2025-06-01T10:00:00Z",
  "synced_at": null
}
```

#### 模板定义 (Template)

```json
{
  "template_id": "anniversary_countdown_vibrant",
  "type": "anniversary",
  "mode": "countdown",
  "theme": "holiday",
  "name": "放假倒计时 · 活力",
  "description": "大字数字 + 活力渐变背景，适合假期倒计时",
  "style_presets": [
    { "id": "vibrant_orange", "name": "活力橙", "preview": "..." },
    { "id": "ocean_blue", "name": "海洋蓝", "preview": "..." },
    { "id": "forest_green", "name": "森林绿", "preview": "..." },
    { "id": "minimal_dark", "name": "极简暗", "preview": "..." }
  ],
  "param_schema": {
    "title": { "type": "string", "max_length": 20, "required": true },
    "target_date": { "type": "datetime", "required": true },
    "subtitle": { "type": "string", "max_length": 30, "required": false, "ai_generated": true },
    "show_hours": { "type": "boolean", "default": false }
  }
}
```

#### 每日新闻组件模板

```json
{
  "template_id": "daily_news_card",
  "type": "news",
  "name": "每日新闻",
  "description": "每天自动更新的新闻摘要卡片",
  "style_presets": [
    { "id": "clean_dark", "name": "简约暗", "preview": "..." },
    { "id": "classic_light", "name": "经典亮", "preview": "..." }
  ],
  "param_schema": {
    "category": { "type": "enum", "values": ["general", "tech", "auto"], "default": "general" },
    "max_items": { "type": "integer", "default": 5, "min": 3, "max": 8 }
  },
  "data_source": "cloud_news_api",
  "refresh_interval": "daily"
}
```

#### 闹钟组件模板

```json
{
  "template_id": "alarm_clock_card",
  "type": "alarm",
  "name": "闹钟",
  "description": "显示下一个闹钟时间 + 快捷设置",
  "style_presets": [
    { "id": "digital_neon", "name": "数码霓虹", "preview": "..." },
    { "id": "analog_minimal", "name": "极简表盘", "preview": "..." }
  ],
  "param_schema": {
    "alarm_time": { "type": "time", "required": true },
    "label": { "type": "string", "max_length": 15, "required": false },
    "repeat": { "type": "enum", "values": ["none", "weekdays", "weekends", "daily"], "default": "none" }
  }
}
```

### 4.3 JSBridge API（v0.1 白名单）

```typescript
interface AIWidgetBridge {
  // 时间
  getDateTime(): Promise<{ timestamp: number; timezone: string }>;
  
  // 通知 & 闹钟
  scheduleNotification(params: {
    id: string;
    title: string;
    body: string;
    trigger_at: number;      // unix timestamp
    repeat?: 'daily' | 'weekdays' | 'weekends' | 'weekly' | 'none';
  }): Promise<{ success: boolean }>;
  
  cancelNotification(id: string): Promise<{ success: boolean }>;
  
  setAlarm(params: {
    hour: number;            // 0-23
    minute: number;          // 0-59
    label?: string;
    repeat?: 'none' | 'weekdays' | 'weekends' | 'daily';
  }): Promise<{ success: boolean; alarm_id: string }>;
  
  // 本地存储（组件级隔离）
  storageGet(key: string): Promise<string | null>;
  storageSet(key: string, value: string): Promise<{ success: boolean }>;
  
  // 主题
  getTheme(): Promise<{ mode: 'light' | 'dark'; accent_color: string }>;
  onThemeChange(callback: (theme: { mode: string; accent_color: string }) => void): void;
  
  // 车辆基础信息（只读）
  getVehicleInfo(): Promise<{
    mileage_km: number;
    battery_percent: number;
    range_km: number;
  }>;
  
  // 网络请求（受限，仅允许白名单域名）
  fetchData(url: string): Promise<{ data: string; status: number }>;
  // 白名单: 仅允许访问自家新闻聚合API
}
```

### 4.4 迭代路线图

```
v0.1 Demo（当前）             v0.5 内测                v1.0 公开发布              v2.0 生态
─────────────────────────────────────────────────────────────────────────────────────►

模板 + 参数化                  模板 + 微编辑              AI 自由生成               组合 + 自动化
4 种组件                      8-12 种组件                20+ 种组件                无限
  纪念日(3主题)                 + 天气/车况/音乐            + 社区分享                + 模板市场
  每日新闻                      + 自定义背景图              + 一键使用他人作品         + 付费风格包
  闹钟                         + 配图上传                  + 排行榜/挑战赛            + 第三方开发者
行车桌面卡片                    + 驻车桌面                                           + 组件串联
手机端创建                      + 语音创建(车端)
手动切换                        + 智能排序
L1 JSON 生成                   + L2 局部CSS生成            + L3 完整H5生成
```

**各版本核心验证假设**：

| 版本 | 验证假设 | 关键指标 |
|------|---------|---------|
| v0.1 | 技术链路可行，高层认可方向 | Demo 演示通过，获得立项 |
| v0.5 | 用户愿意创建和使用 | 创建率 > 30%，周活使用率 > 50% |
| v1.0 | 用户愿意分享，飞轮能转起来 | 分享率 > 10%，通过分享获客占比 > 20% |
| v2.0 | UGC 生态可持续 | 月活创作者占比 > 5%，社区组件月增长 > 100 |

---

## 五、预计成本

### 5.1 v0.1 Demo 人力成本

| 角色 | 人数 | 投入周期 | 主要工作 |
|------|------|---------|---------|
| 产品经理 | 1 | 6 周 | 需求定义、模板规划、用户故事设计、Demo 演示策略 |
| 前端开发（H5 模板 + 手机端） | 1-2 | 5 周 | H5 组件模板开发（4种组件×多风格）、手机端 Web App、车端 WebView 集成 |
| 后端/AI 开发 | 1 | 4 周 | AI 生成 Pipeline、Prompt 工程、云端同步服务、新闻聚合服务 |
| 设计师 | 0.5 | 3 周 | Design Tokens 输出、模板视觉设计（4种组件×每种3-4风格）、逐一审核 |
| 车端开发（兼） | 0.5 | 2 周 | WebView 容器、JSBridge 实现、桌面卡片接入、闹钟能力 |
| 测试 | 0.5 | 2 周 | 联调测试、车端适配验证、稳定性测试 |

**合计**：约 4-5 人，6 周。

### 5.2 云端/AI 成本（按 Demo 阶段估算）

| 项目 | 单价估算 | Demo 阶段用量 | 月成本 |
|------|---------|-------------|--------|
| LLM API 调用 | ~¥0.1/次（GPT-4o mini 级别，JSON Mode） | 500 次/月（内部测试） | ~¥50 |
| 新闻 API | 免费额度 / ~¥100/月 | 每日数次调用 | ~¥100 |
| 对象存储 | ~¥0.12/GB/月 | ≤1GB | ~¥1 |
| 云服务器 | ~¥200/月（轻量 ECS） | 1 台 | ~¥200 |
| 推送服务 | 复用已有 MQTT 通道 | — | ¥0 |

**Demo 阶段云端月成本 ≈ ¥400 以内**，几乎可忽略。

### 5.3 规模化后的成本模型（参考）

假设 v1.0 公开发布后月活 10 万用户：

| 项目 | 估算 |
|------|------|
| LLM API | 每用户月均生成 3 次 → 30 万次/月 → ~¥3 万/月 |
| 新闻 API + AI 摘要 | 每日更新 + 缓存分发 → ~¥0.5 万/月 |
| 对象存储 + CDN | 每组件 ~100KB → ~¥1 万/月 |
| 服务器 | 多实例扩容 → ~¥1 万/月 |
| **月总成本** | **~¥5.5 万/月** |

**成本可控**：组件是静态 H5 包，生成后几乎零运行成本。新闻组件通过云端缓存分发，不会随用户数线性增长。主要成本在 AI 生成调用上，可通过缓存热门模板结果、降级模型等方式优化。

---

## 附录 A：v0.1 H5 组件模板文件结构

```
widget_bundle.zip
├── index.html          # 入口页面
├── style.css           # 组件样式（引用 design-tokens）
├── tokens.css          # Design Tokens（由平台注入，组件不可覆盖）
├── main.js             # 组件逻辑（Vanilla JS）
├── bridge.js           # JSBridge 封装（平台提供，统一版本）
├── manifest.json       # 组件元数据（类型、参数、版本）
└── assets/             # 静态资源（图片等，可选）
    └── bg.webp
```

**manifest.json 示例**：
```json
{
  "widget_id": "comp_uuid",
  "type": "anniversary",
  "version": "1.0.0",
  "min_bridge_version": "0.1.0",
  "permissions": ["getDateTime", "storageGet", "storageSet"],
  "display": {
    "width": 896,
    "height": 1464,
    "supports_dark_mode": true
  }
}
```

## 附录 B：Prompt 工程参考（v0.1 L1 方案）

```
## System Prompt

你是一个车载小组件参数生成助手。用户会告诉你他们选择的模板类型和已填参数，
你的任务是补全缺失参数（尤其是副标题等文案字段），并输出完整的 JSON。

### 规则
1. 只输出 JSON，不要任何其他内容（无 markdown、无解释）
2. 所有字段必须符合对应模板的 param_schema
3. 副标题(subtitle)要简短、有情感、贴合主题，≤30字
4. 闹钟标签(label)要简洁实用，≤15字
5. 不得生成任何不当、违法、暴力、色情内容

### 可用组件类型
1. anniversary - 纪念日（正数模式：已过X天 / 倒数模式：还有X天）
   - 主题: love(恋爱), baby(宝宝成长), holiday(放假倒计时)
2. news - 每日新闻（展示新闻摘要列表）
3. alarm - 闹钟（显示下一个闹钟 + 快捷设置）

### 输出格式
{
  "component_type": "anniversary | news | alarm",
  "mode": "countdown | countup",        // 仅 anniversary
  "theme": "love | baby | holiday",      // 仅 anniversary
  "template_id": "从可用模板中选择",
  "style_preset": "从该模板的可用风格中选择",
  "params": { ... 按模板 param_schema 填充 }
}

### 文案生成风格指引
- 恋爱主题: 温暖甜蜜，如"每一天都算数"、"最好的时光是有你的日子"
- 宝宝主题: 温柔治愈，如"每一天都是新的奇迹"、"慢慢长大不着急"
- 放假主题: 活泼期待，如"假期在向你招手"、"再坚持一下就放假啦"

### 可用模板清单
[此处注入所有模板的 template_id + param_schema + style_presets]
```

## 附录 C：Demo 验收 Checklist

**AI 生成**
- [ ] 纪念日 3 个主题各 5 组不同参数，JSON 生成合法率 ≥ 99%
- [ ] AI 生成的副标题/文案自然、贴切，主观评分 ≥ 7/10
- [ ] 从点击"生成"到预览展示 ≤ 5s（P95）
- [ ] 内容安全规则有效：输入不当内容时正确拦截

**视觉品质**
- [ ] 每种组件×每种风格，在暗色模式下无样式错乱
- [ ] 每种组件×每种风格，在浅色模式下无样式错乱
- [ ] 不同长度的标题/副标题不会"破版"（截断/缩字处理正确）
- [ ] 设计师逐一审核签字确认

**车端体验**
- [ ] H5 组件首屏渲染 ≤ 2s，无白屏闪烁
- [ ] 组件连续运行 8 小时无崩溃、无内存泄漏
- [ ] 手动切换（← →）响应流畅，切换动画无卡顿
- [ ] 圆点指示器正确反映当前位置
- [ ] 重启后恢复到上次查看的组件
- [ ] 新闻组件每日正确更新内容
- [ ] 闹钟组件能正确触发系统闹钟

**跨端链路**
- [ ] 手机端预览效果与车端实际展示一致（896×1464）
- [ ] 手机点击同步 → 车端卡片更新 ≤ 5s（车机在线时）
- [ ] 车机断网后已缓存的组件正常展示（离线支持）
- [ ] 车机重新联网后自动同步未接收的组件

**Demo 演示**
- [ ] 3 分钟演示脚本走完无卡顿，每个步骤有明确反馈
- [ ] 准备 3-5 个预生成的精品组件用于快速展示多样性
- [ ] 准备 1 个现场实操流程，从选模板到车端亮起 ≤ 1 分钟
