[build]
  command = "npm run build"
  publish = "dist"

# API 代理到后端（部署后端后替换 YOUR_RENDER_URL 为实际地址）
[[redirects]]
  from = "/api/*"
  to = "https://ai-widget-api.onrender.com/api/:splat"
  status = 200
  force = true

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
