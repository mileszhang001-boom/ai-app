services:
  - type: web
    name: ai-widget-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: USE_MOCK
        value: "true"
      - key: QWEN_API_KEY
        sync: false
